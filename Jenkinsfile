pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building website'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing website'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying website'
            }
        }
    }
}
